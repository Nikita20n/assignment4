#include<stdio.h>
#include<conio.h>
void main()
{
       long int n,reverse=0,rem;
       clrscr();
       printf("enter a number:");
       scanf("%ld",&n);
       while(n>0)
       {
	 rem=n%10;
	 reverse=reverse*10+rem;
	 n/=10;
	 printf("%ld\n",rem);
       }
       printf("reverse number=%ld",reverse);
       getch();
}